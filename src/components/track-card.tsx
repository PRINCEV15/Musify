"use client";import {Heart,MoreHorizontal,Play,Repeat2} from "lucide-react";import {Track} from "@/lib/types";import {usePlayer} from "./player-provider";import Link from "next/link";export function TrackCard({track}:{track:Track}){const{play}=usePlayer();return <article className="card"><div className="art"><img src={track.cover} alt=""/><button className="playOverlay" onClick={()=>play(track)} aria-label={"Play "+track.title}><Play size={19} fill="currentColor"/></button></div><div className="meta"><Link href={"/track/"+track.slug} style={{color:"inherit",textDecoration:"none"}}><div className="title">{track.title}</div></Link><div className="sub">{track.artist} · {track.plays} plays</div><div style={{display:"flex",gap:6,marginTop:10}}><button className="iconbtn"><Heart size={15}/></button><button className="iconbtn"><Repeat2 size={15}/></button><button className="iconbtn"><MoreHorizontal size={15}/></button></div></div></article>}