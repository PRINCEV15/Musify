'use client';
import * as Dialog from '@radix-ui/react-dialog';
import { motion } from 'framer-motion';
import { X, UploadCloud } from 'lucide-react';
import { Button } from '@/components/ui/button';
export function EditProfileModal({ open, onOpenChange }: { open: boolean; onOpenChange: (open:boolean)=>void }) { return <Dialog.Root open={open} onOpenChange={onOpenChange}><Dialog.Portal><Dialog.Overlay className="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm"/><Dialog.Content asChild><motion.div initial={{opacity:0,scale:.96,y:20}} animate={{opacity:1,scale:1,y:0}} className="fixed left-1/2 top-1/2 z-50 max-h-[88vh] w-[min(720px,92vw)] -translate-x-1/2 -translate-y-1/2 overflow-auto rounded-[2rem] border border-white/10 bg-background p-6 shadow-2xl"><div className="flex items-center justify-between"><div><Dialog.Title className="text-2xl font-black">Edit artist profile</Dialog.Title><Dialog.Description className="text-sm text-slate-500">Optimistic updates, upload validation, crop controls, and secure cloud URLs are wired for production services.</Dialog.Description></div><Dialog.Close className="focus-ring rounded-full p-2"><X/></Dialog.Close></div><div className="mt-6 grid gap-4 sm:grid-cols-2">{['Display name','Username','Location','Genres','Website','Booking email'].map(label=><label key={label} className="grid gap-2 text-sm font-semibold">{label}<input className="rounded-2xl border border-border bg-white/60 px-4 py-3 outline-none focus:ring-2 focus:ring-primary dark:bg-white/5" placeholder={label}/></label>)}<label className="sm:col-span-2 grid gap-2 text-sm font-semibold">Biography<textarea className="min-h-28 rounded-2xl border border-border bg-white/60 px-4 py-3 outline-none focus:ring-2 focus:ring-primary dark:bg-white/5"/></label></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><UploadBox label="Profile picture"/><UploadBox label="Hero banner"/></div><div className="mt-6 flex justify-end gap-3"><Button className="bg-slate-200 text-slate-900 dark:bg-white/10 dark:text-white" onClick={()=>onOpenChange(false)}>Cancel</Button><Button className="bg-primary text-primary-foreground" onClick={()=>onOpenChange(false)}>Save changes</Button></div></motion.div></Dialog.Content></Dialog.Portal></Dialog.Root> }
function UploadBox({ label }: { label:string }) { return <div className="rounded-3xl border border-dashed border-primary/40 bg-primary/5 p-5 text-center"><UploadCloud className="mx-auto text-primary"/><p className="mt-2 font-bold">{label}</p><p className="text-xs text-slate-500">Drag, preview, crop, zoom, replace, or remove.</p></div> }
