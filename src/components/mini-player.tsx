"use client";import {Pause,Play,SkipBack,SkipForward,Heart,ListMusic,Volume2} from "lucide-react";import {usePlayer} from "./player-provider";export function MiniPlayer(){const{track,playing,progress,toggle,seek}=usePlayer();if(!track)return null;return <div className="player"><div className="container playerInner"><div className="now"><img src={track.cover} alt=""/><div style={{minWidth:0}}><div className="title">{track.title}</div><div className="sub">{track.artist}</div></div></div><div><div className="controls"><button className="iconbtn"><SkipBack size={17}/></button><button className="mainPlay" onClick={toggle}>{playing?<Pause size={18}/>:<Play size={18}/>}</button><button className="iconbtn"><SkipForward size={17}/></button></div><div className="progress" onClick={e=>{const r=e.currentTarget.getBoundingClientRect();seek((e.clientX-r.left)/r.width)}}><div className="progressLine"><span style={{width:(progress*100)+"%"}}/></div></div><div className="playerRight"><button className="iconbtn"><Heart size={17}/></button><button className="iconbtn"><ListMusic size={17}/></button><button className="iconbtn"><Volume2 size={17}/></button></div></div></div></div>}