import {tracks} from "@/lib/mock-data";import {TrackCard} from "@/components/track-card";export default function Library(){return <div className="container page"><div className="hero" style={{paddingTop:5}}><div className="eyebrow">Your library</div><h1>Keep what moves you.</h1><p>Liked tracks, playlists, albums, artists and listening history in one place.</p></div><div className="section"><div className="sectionHead"><h2>Liked tracks</h2></div><div className="grid">{tracks.map(t=><TrackCard key={t.id} track={t}/>)}</div></div></div>}