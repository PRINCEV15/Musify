import {tracks} from "@/lib/mock-data";import {TrackCard} from "@/components/track-card";export default function Discover(){return <div className="container page"><div className="hero" style={{paddingTop:10}}><div className="eyebrow">Discovery</div><h1>Go somewhere new.</h1><p>Fresh uploads, underground sounds and artists on the rise.</p></div><div className="section"><div className="sectionHead"><h2>Trending now</h2></div><div className="grid">{tracks.map(t=><TrackCard key={t.id} track={t}/>)}</div></div><div className="section"><div className="sectionHead"><h2>Fresh uploads</h2></div><div className="grid">{tracks.slice().reverse().map(t=><TrackCard key={t.id} track={t}/>)}</div></div></div>}