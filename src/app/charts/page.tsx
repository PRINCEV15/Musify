import {tracks} from "@/lib/mock-data";import {TrackCard} from "@/components/track-card";export default function Charts(){return <div className="container page"><div className="hero" style={{paddingTop:5}}><div className="eyebrow">Charts</div><h1>What the world is playing.</h1><p>Editorial and engagement-led chart surfaces designed for daily, weekly and monthly ranking jobs.</p></div><div className="section"><div className="sectionHead"><h2>Top tracks</h2><span className="pill">This week</span></div><div className="grid">{tracks.map((t,i)=><div key={t.id}><div className="pill" style={{marginBottom:8}}>#{i+1}</div><TrackCard track={t}/></div>)}</div></div></div>}