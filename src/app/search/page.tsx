"use client";import {useState} from "react";import {tracks} from "@/lib/mock-data";import {TrackCard} from "@/components/track-card";export default function SearchPage(){const[q,setQ]=useState("");const result=tracks.filter(t=>[t.title,t.artist,t.genre].some(v=>v.toLowerCase().includes(q.toLowerCase())));return <div className="container page"><div className="hero" style={{paddingTop:5}}><div className="eyebrow">Search</div><h1>Find a sound.</h1><div className="search" style={{margin:0,width:"min(700px,100%)"}}><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="Artists, tracks, albums, playlists..."/></div></div><div className="section"><div className="sectionHead"><h2>{q?"Results for “"+q+"”":"Popular right now"}</h2></div>{result.length?<div className="grid">{result.map(t=><TrackCard key={t.id} track={t}/>)}</div>:<div className="empty">No matches yet. Try another artist, track or genre.</div>}</div></div>}