import {tracks,artists,playlists} from "@/lib/mock-data";import {TrackCard} from "@/components/track-card";import Link from "next/link";function Section({title,children}:{title:string;children:React.ReactNode}){return <section className="section"><div className="sectionHead"><h2>{title}</h2><Link href="/discover">See all</Link></div>{children}</section>}export default function Home(){return <div className="container"><section className="hero"><div className="eyebrow">Where music finds you.</div><h1>Good evening.<br/>Find your next favorite.</h1><p>A living stream of independent artists, fresh releases, deep cuts and sounds worth sharing.</p></section><Section title="Continue listening"><div className="grid">{tracks.map(t=><TrackCard key={t.id} track={t}/>)}</div></Section><Section title="Made for you"><div className="grid">{tracks.slice().reverse().map(t=><TrackCard key={t.id} track={t}/>)}</div></Section><Section title="Rising artists"><div className="grid">{artists.map(a=><Link key={a.id} href={"/artist/"+a.slug} className="card" style={{textDecoration:"none",color:"inherit"}}><div className="art"><img src={a.avatar} alt=""/></div><div className="meta"><div className="title">{a.name} {a.verified?"✓":""}</div><div className="sub">{a.followers} followers · {a.genre}</div></div></Link>)}</div></Section><Section title="Editorial playlists"><div className="grid">{playlists.map(p=><div key={p.id} className="card"><div className="art"><img src={p.cover} alt=""/></div><div className="meta"><div className="title">{p.title}</div><div className="sub">{p.creator} · {p.tracks} tracks</div></div></div>)}</div></Section></div>}