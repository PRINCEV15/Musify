import "./globals.css";import {PlayerProvider} from "@/components/player-provider";import {SiteHeader} from "@/components/site-header";import {MiniPlayer} from "@/components/mini-player";export const metadata={title:"Musify — Where music finds you.",description:"Discover. Create. Connect."};export default function RootLayout({children}:{children:React.ReactNode}){return <PlayerProvider><div className="shell"><SiteHeader/><main>{children}</main><MiniPlayer/></div></PlayerProvider>}